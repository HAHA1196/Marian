create database if not exists marian default character set utf8;

use marian;

create table if not exists
 products (
    productId int auto_increment primary key, 
    productStyleNumber varchar(10), 
    productClass varchar(10), 
    productName varchar(40), 
    productDescription text, 
    productImg blob, 
    productSize varchar(10) null, 
    productPrice smallint,
    productInStock smallint
);
create table if not exists
 customers (
    customerId int auto_increment primary key, 
    customerName varchar(40), 
    customerGender tinyint(1), 
    customerPhone varchar(24), 
    customerEmail varchar(100), 
    customerAddress varchar(255)
);
create table if not exists
 orders (
    orderId int auto_increment primary key, 
    customerId int, 
    orderDate datetime
);
create table if not exists
 orderdetails (
    orderId int primary key, 
    productId int, 
    productPrice smallint, 
    quantity smallint
);
create table if not exists
 news (
    newsId int auto_increment primary key, 
    newsTitle varchar(50),  
    newsDate datetime, 
    newsCoverImg blob 
);
create table if not exists
 newsContent (
    newsId int, 
    newsSubtitle varchar(50), 
    newsArticle text, 
    newsImg blob, 
    newsFigcaption text 
)

insert into news ()